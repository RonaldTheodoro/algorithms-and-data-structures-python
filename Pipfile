[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
pytest = "*"
pytest-cov = "*"

[requires]
python_version = "3.9"

[scripts]
test = "pytest"
test_report = "pytest -c pytest_cov.ini"
